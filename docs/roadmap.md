# Roadmap

After stabilizing the MVP: persistent compiler experiments behind an off-by-default interface, richer structured previews, memory limits, signed desktop packaging, multi-file workspaces, LLDB integration, WASM execution experiments and stronger OS-native sandbox profiles. None are part of the current security or compatibility contract.
