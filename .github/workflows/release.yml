# Release: a single button (workflow_dispatch) that versions the app in
# every manifest, commits + tags, builds the desktop bundles for Linux
# (AppImage/deb/rpm, x86_64) and macOS (dmg, Apple Silicon) and publishes
# the GitHub Release with generated notes. Pushes made with GITHUB_TOKEN
# do not re-trigger workflows, so there is no recursion.
name: Release

on:
  workflow_dispatch:
    inputs:
      bump:
        description: "Version bump (semver)"
        required: true
        type: choice
        default: patch
        options: [patch, minor, major]

permissions:
  contents: write

env:
  ZIG_VERSION: 0.16.0

jobs:
  version:
    name: Bump + tag v-next
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.bump.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Bump every manifest
        id: bump
        run: |
          VERSION="$(node scripts/bump-version.mjs '${{ inputs.bump }}')"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
      - name: Commit and tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -am "chore(release): v${{ steps.bump.outputs.version }}"
          git tag -a "v${{ steps.bump.outputs.version }}" -m "Atomis v${{ steps.bump.outputs.version }}"
          git push origin HEAD "refs/tags/v${{ steps.bump.outputs.version }}"

  build:
    name: Bundle ${{ matrix.artifact }}
    needs: version
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            bundle: linux
            artifact: linux-x86_64
          - os: macos-latest
            bundle: mac
            artifact: macos-aarch64
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v${{ needs.version.outputs.version }}
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - uses: mlugg/setup-zig@v2
        with:
          version: ${{ env.ZIG_VERSION }}
      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: false
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: release
          workspaces: |
            apps/server-rs
            rust/instrumenter
            apps/desktop/src-tauri
      - name: Tauri Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev build-essential curl wget file \
            libxdo-dev libssl-dev libayatana-appindicator3-dev \
            librsvg2-dev patchelf xdg-utils libfuse2
      - run: pnpm install --frozen-lockfile
      - name: Build sidecar + resources (pnpm desktop:build)
        run: pnpm desktop:build
      - name: Bundle (${{ matrix.bundle }})
        run: pnpm --filter @atomis/desktop bundle:${{ matrix.bundle }}
      - name: Collect bundles
        shell: bash
        run: |
          BUNDLES=apps/desktop/src-tauri/target/release/bundle
          mkdir -p dist-release
          if [ "${{ matrix.bundle }}" = "linux" ]; then
            cp "$BUNDLES"/appimage/*.AppImage dist-release/
            cp "$BUNDLES"/deb/*.deb dist-release/
            cp "$BUNDLES"/rpm/*.rpm dist-release/
          else
            cp "$BUNDLES"/dmg/*.dmg dist-release/
          fi
          ls -l dist-release
      - uses: actions/upload-artifact@v4
        with:
          name: bundles-${{ matrix.artifact }}
          path: dist-release/*
          if-no-files-found: error

  publish:
    name: Publish GitHub Release
    needs: [version, build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist-release
          merge-multiple: true
      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ needs.version.outputs.version }}
        run: |
          cat > notes.md <<'NOTES'
          Desktop bundles (Tauri v2, ~5 MB Rust sidecar):

          - **Linux x86_64**: AppImage, .deb and .rpm
          - **macOS Apple Silicon**: .dmg — unsigned: the first time, open
            it via right click → Open, or run
            `xattr -d com.apple.quarantine /Applications/atomis.app`

          Language toolchains (zig, cargo, go, node, python3, clang) come
          from the host machine; without them the app starts degraded per
          language.
          NOTES
          gh release create "v$VERSION" dist-release/* \
            --repo "$GITHUB_REPOSITORY" \
            --title "Atomis v$VERSION" \
            --notes-file notes.md \
            --generate-notes
